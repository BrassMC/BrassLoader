pluginManagement {
    repositories {
        gradlePluginPortal()
        maven {
            url 'https://repo.repsy.io/mvn/brassmc/public'
        }
    }
}

rootProject.name = 'BrassLoader'
if (new File(rootDir, 'BrassAPI').exists())
    includeBuild './BrassAPI'